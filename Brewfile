# -- deps --
# go lang for aws lambda fn
brew "go"

# manage aws infrastructure w/ code
brew "terraform"

# -- deps/dev --
# containers (for localstack)
cask "docker"

# mock aws services for local dev
brew "localstack"

# aws cli for local testing
brew "awscli"
